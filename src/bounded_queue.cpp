

// src/bounded_queue.cpp
// Intentionally empty translation unit.
// The global termination flag `g_terminate` is defined in main.cpp
// and declared `extern` in bounded_queue.hpp so all TUs share it.
